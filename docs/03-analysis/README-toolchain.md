# 2026 Web Toolchain Upgrade

This branch evaluates Next.js 16, Tailwind CSS 4, and ESLint 10 together with current compatible supporting libraries.

Validation gates: `npm ci`, `npm run lint`, `npm run build`, static export verification, and visual regression review.

`next-mdx-remote` is tracked separately because its upstream repository is archived.
