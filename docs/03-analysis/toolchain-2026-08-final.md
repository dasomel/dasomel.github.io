# 2026 Web Toolchain Migration

Next.js 16, Tailwind CSS 4, and ESLint 10 are being evaluated together as a coherent modern web stack. CI must pass lint, full static export, and visual regression checks before merge.

The existing `next-mdx-remote` dependency is treated as a separate migration because its upstream repository is archived.
