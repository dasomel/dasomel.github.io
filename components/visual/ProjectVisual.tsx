const visuals:Record<string,{label:string;title:string;items:string[]}>= {
 narwhal:{label:'PLATFORM MAP',title:'One login, one GitOps path, many platform capabilities',items:['Portal + Keycloak SSO','Argo CD + Gitea GitOps','Cilium + Istio + APISIX','Prometheus · Loki · Tempo · Hubble']},
 beluga:{label:'DATA FLOW',title:'Streaming to lakehouse to analytics',items:['Kafka + Debezium','Flink processing','Iceberg + Lakekeeper','Trino + Superset + Airflow']},
 kubemetal:{label:'HYBRID ML',title:'Kubernetes control plane + macOS Metal compute',items:['Tauri desktop','K3s control plane','MLflow + SeaweedFS','MLX LoRA + serving']}
};
export function ProjectVisual({slug}:{slug:string}){const v=visuals[slug];if(!v)return null;return <figure className="rounded-2xl border p-5 sm:p-6" style={{borderColor:'var(--border)',background:'linear-gradient(135deg,var(--surface),var(--surface-hi))'}}><div className="font-mono text-[10px] tracking-[.12em]" style={{color:'var(--accent)'}}>{v.label} / VISUAL EVIDENCE</div><h2 className="mt-2 max-w-3xl text-xl font-semibold sm:text-2xl" style={{color:'var(--text)'}}>{v.title}</h2><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{v.items.map((item,i)=><div key={item} className="relative overflow-hidden rounded-xl border p-4" style={{borderColor:'var(--border)',backgroundColor:'var(--bg)'}}><div className="absolute right-3 top-2 font-mono text-4xl font-semibold opacity-10" style={{color:'var(--accent)'}}>{String(i+1).padStart(2,'0')}</div><div className="h-2 w-12 rounded-full" style={{backgroundColor:i%3===0?'#72E3C3':i%3===1?'#55B8DB':'#F0A35A'}}/><div className="mt-8 text-sm font-semibold" style={{color:'var(--text)'}}>{item}</div></div>)}</div><figcaption className="mt-4 text-xs leading-5" style={{color:'var(--text-muted)'}}>A compact visual summary; the architecture diagram below explains the execution flow in more detail.</figcaption></figure>}
